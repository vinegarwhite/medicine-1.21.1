package com.medicine.item.custom.food.foods;

import com.medicine.item.custom.food.MedicineFoodItem;

public class TomatoPasta extends MedicineFoodItem {
    public TomatoPasta(Settings settings) {
        super(settings);
    }

}
